const CryptoRouterFlow = function (routeTable, middlewares, routers, initialRoute) {
    for (let i = 0; i < routers.length; i++) {
        routers[i].routeTables;
        if (routers[i].routers.length > 0) {
        }
        else {
            routers[i].routeTables;
        }
    }
};
//# sourceMappingURL=CryptoLiteRouterFlow.js.map