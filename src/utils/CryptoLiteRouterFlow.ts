const CryptoRouterFlow=function(routeTable:object,middlewares:Array<Function>,routers:Array<any>,initialRoute?:object){

    for(let i =0;i<routers.length;i++){
            routers[i].routeTables
        if(routers[i].routers.length>0){

        }
        else{
            routers[i].routeTables
        }
    }
}